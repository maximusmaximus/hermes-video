---
name: video-review-gate
description: Present interactive inline Telegram review buttons and distribute video deliverables via Cloudflare Tunnels (trycloudflare.com).
---

# Video Review Gate Skill

Allows autonomous agents to present decisions, rough cuts, and master renders to the user via Telegram interactive buttons, backed by external Cloudflare streaming links.

## Principles

1. **No Chat Spam**: Do not output endless JSON or logs to Telegram chat.
2. **Interactive Decisions**: Use inline keyboard buttons (`[✅ Approve]`, `[🔄 Reroll]`, `[✂️ Revise]`).
3. **Cloudflare Distribution**: Deliver video previews and project archives via ephemeral `trycloudflare.com` links generated by `secure-share`.

## Python Usage

```python
from src.telegram.gateway import TelegramGateway

gateway = TelegramGateway()
gateway.send_render_review_gate(
    project_title="VØIDRIDE // CHROMA MORGUE",
    track_title="01 COLD INCISION",
    duration=15.0,
    cloudflare_url="https://preview-video.trycloudflare.com/..."
)
```
